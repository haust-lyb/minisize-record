# Enable hoisting for better compatibility with native modules
hoist-pattern=*
# Enable public-hoist-pattern for electron and related packages
public-hoist-pattern=*
# Shim for electron to work with pnpm
enable-pre-post-scripts=true
